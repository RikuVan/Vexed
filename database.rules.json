{
  "rules": {
    "V0": {
      "players": {
        "$uid": {
          "game": {
            ".write": "auth !== null && auth.provider === 'google' && $uid === auth.uid",
            ".read": "auth !== null && auth.provider === 'google' && $uid === auth.uid"
          }
        }
      }
    }
  }
}
